

services:
  pgvector:
    image: pgvector/pgvector:pg17
    container_name: infraai-pgvector
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: salim077
      POSTGRES_DB: infraai
    ports:
      - "5433:5432"
    volumes:
      - infraai_pgdata:/var/lib/postgresql/data

volumes:
  infraai_pgdata: